import "./Header.scss";

const Header = () => {
  return (
    <div>
      <header className="header">
        <button className="header__option">My Collection</button>
      </header>
    </div>
  );
};

export default Header;
