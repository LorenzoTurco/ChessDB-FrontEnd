import "./Header.scss";

const Header = () => {
  return (
    <div>
      <header className="header">
        <button className="header__title">My Collection</button>
      </header>
    </div>
  );
};

export default Header;
